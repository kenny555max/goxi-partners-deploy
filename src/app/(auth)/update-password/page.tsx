import React from 'react';
import PasswordUpdateForm from "@/components/templates/password-update-from";

export const metadata = {
    title: 'Update Password - Goxi',
    description: 'Update your password for Goxi account',
};

export default function UpdatePasswordPage() {
    return <PasswordUpdateForm />;
}